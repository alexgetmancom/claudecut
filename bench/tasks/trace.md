In the repository in the current directory, find where configuration or
settings are loaded from disk.

Report, in at most 10 lines: the file and line where it happens, the format
read, and what the code does when the file is missing or malformed. Quote the
single most relevant line. If there is no such code, say so plainly.

Do not modify anything.
